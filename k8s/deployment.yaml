apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app-deployment
spec:
  # REMOVED 'replicas: 3' to let HPA handle scaling
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
    spec:
      containers:
      - name: flask-app
        image: daddyohyeah/my-app:latest
        imagePullPolicy: Always 
        ports:
        - containerPort: 5000

        # RESOURCES: Prevents app from crashing server
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        
        # PROBES: Updated to use /health
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
  
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3